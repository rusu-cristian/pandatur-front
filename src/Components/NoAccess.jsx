export const NoAccess = () => {
  return (
    <div
      style={{
        textAlign: "center",
        marginTop: "50px",
        fontSize: "18px",
        color: "red"
      }}
    >
      <h2>No access page!</h2>
    </div>
  )
}
